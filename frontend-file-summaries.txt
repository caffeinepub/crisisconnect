{
  "frontend/src/App.tsx": "Main router setup with all routes including the new /emergency-contacts route mapped to EmergencyContactsPage under the authenticated AppLayout.",
  "frontend/src/pages/HospitalFinderPage.tsx": "Hospital finder page that fetches hospitals from both the backend and Overpass API, merges and deduplicates results, computes distances using haversine, and displays sortable/filterable cards with address, phone, bed availability badges, emergency indicators, and Google Maps directions links.",
  "frontend/src/pages/BloodDonorPage.tsx": "Blood donor registry page with fixed default import for DonorCard, corrected form submission logic (removes invalid `\"success\" in result` check since mutateAsync returns bigint directly and throws on error), image-file manual blood type selector, and strict regex auto-detection for text files.",
  "frontend/src/pages/VolunteerPage.tsx": "Volunteer management page with proof document upload in the registration form, clickable volunteer cards that open the VolunteerDetailModal, and filterable volunteer directory.",
  "frontend/src/pages/ResourceMapPage.tsx": "Full-screen map page rewritten to use CDN-loaded Leaflet (no npm react-leaflet/leaflet imports) with a plain div container, imperative marker management, Overpass API hospital fetching merged with backend hospitals filtered to 5 km, and leaflet-routing-machine for in-map turn-by-turn directions with a red route line.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend interactions; updated `useAddBloodDonor` to accept `proofText` and `detectedBloodType` instead of a manual blood type, and `useRegisterVolunteer` to pass the full volunteer object including `proofText`.",
  "frontend/src/components/sos/SOSModal.tsx": "SOS modal with countdown, geolocation, and backend recording; fixed to call `useGetMyEmergencyContacts()` with no arguments and `recordSOS.mutateAsync` with only `lat`/`lng` fields (matching the updated Omit type).",
  "frontend/src/pages/EmergencyContactsPage.tsx": "Emergency contacts management page updated to call `useGetMyEmergencyContacts()` with no arguments, matching the updated hook signature.",
  "frontend/src/components/layout/AppLayout.tsx": "App shell with sticky header, navigation (including \"Emergency Contacts\" link), SOS button, user profile display, logout, mobile menu, footer (copyright only, attribution removed), profile setup modal, chatbot, and toast provider.",
  "frontend/src/components/alerts/AlertFeed.tsx": "Alert feed component that displays emergency alerts using the renamed `useGetEmergencyAlerts` hook with loading skeletons and time-ago formatting.",
  "frontend/src/pages/DashboardPage.tsx": "Dashboard page updated to use the renamed `useGetDashboardStats` hook for displaying live emergency resource statistics.",
  "frontend/src/components/volunteer/VolunteerDetailModal.tsx": "New modal component that displays full volunteer details including name, city, skills as colored badges, active/inactive status, and a proof-submitted indicator when proofText is present.",
  "frontend/src/components/blood/DonorCard.tsx": "Blood donor card component displaying donor name, verified blood type (using `verifiedBloodType` from the backend interface), city, contact, and a proof-submitted indicator when proofText is present.",
  "frontend/src/utils/overpass.ts": "Utility module for querying the Overpass API for nearby hospitals, with functions to build queries, fetch results, parse address/phone/hours/emergency tags, and compute haversine distances.",
  "frontend/src/utils/bloodTypeDetection.ts": "Blood type detection utility with strict priority-ordered regex (AB+/AB- before A+/A-, B+/B-, O+/O-) covering bare symbols, labeled formats, and spelled-out positivity/negativity; exports detectBloodTypeFromText and BLOOD_TYPES constant."
}